 * Renders a ```<div />``` with ```backgroundImage``` options.
 * The div contains a ```<Grid />``` component that can be passes ```props```.
 * ```children``` must be contained in a ```<Grid item />``` component

```js
<Section spacing={3}>
Hi
</Section>
```